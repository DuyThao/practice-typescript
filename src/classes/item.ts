
export class Item {
    public id: string;
    public title: string;

    constructor(id: string, title: string) {
        this.id = id;
        this.title =title;
    }


    addItem(){
        //add vao json data

        return 'list data ';
    }
}